# 签名证书配置
# 复制此文件为 keystore.properties 并填写实际值
# 注意：keystore.properties 已被 .gitignore 排除，不会提交到版本控制

# 证书文件路径（支持绝对路径或相对路径）
storeFile=D\\:\\Desktop\\mykeys.jks

# 证书库密码
storePassword=mykeys

# 证书别名
keyAlias=mykeys

# 别名密码
keyPassword=mykeys
